package database

func createTable() {
	// Migrate the schema
	// err = db.AutoMigrate(&database.Goods{})
	// if err != nil {
	// 	panic("failed to migrate schema")
	// }
	// fmt.Println("Table created successfully")
}

func selectGoods() {
	// Select the record from the database
	// var selectedGood database.Goods
	// err = db.First(&selectedGood, 1).Error
	// if err != nil {
	// 	panic("failed to select record")
	// }
	// fmt.Println("Selected Record:")
	// fmt.Printf("ID: %d\n", selectedGood.ID)
	// fmt.Printf("Name: %s\n", selectedGood.Name)
	// fmt.Printf("New Price: %.2f\n", selectedGood.NewPrice)
	// fmt.Printf("Discount Rate: %.4f\n", selectedGood.DiscountRate)
}
